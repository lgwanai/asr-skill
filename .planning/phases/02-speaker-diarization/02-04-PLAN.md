---
phase: 02-speaker-diarization
plan: 04
type: execute
wave: 3
depends_on:
  - 02-03
files_modified: []
autonomous: false
requirements:
  - AUDIO-02
  - SPKR-01
  - SPKR-02
  - SPKR-03

must_haves:
  truths:
    - "User can verify speaker diarization works correctly"
    - "User can verify video file handling works correctly"
  artifacts: []
  key_links: []
---

<objective>
Verify Phase 2 implementation works end-to-end with real audio/video files.

Purpose: Human verification of speaker diarization and video extraction features. This checkpoint ensures all Phase 2 requirements are met before proceeding to Phase 3.

Output: Verification confirmation from user.
</objective>

<execution_context>
@/Users/wuliang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wuliang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-speaker-diarization/02-CONTEXT.md
@.planning/phases/02-speaker-diarization/02-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify Phase 2 implementation</name>
  <files>N/A</files>
  <action>
Human verification checkpoint for Phase 2 Speaker Diarization.

Verify the following implementations:

1. **Video Support (AUDIO-02)**
   - `asr_skill/preprocessing/video.py` - FFmpeg-based audio extraction
   - Supported formats: MP4, AVI, MKV
   - Auto-cleanup of temp files via context manager

2. **Speaker Diarization (SPKR-01)**
   - CAM++ speaker model integrated into FunASR pipeline
   - Alphabetical speaker labels (Speaker A, Speaker B, etc.)
   - `diarize=True` by default

3. **Speaker Timestamps (SPKR-02)**
   - Each segment includes start/end times per speaker
   - Available in both TXT and JSON output

4. **Overlap Detection (SPKR-03)**
   - Time-based overlap detection (>50% threshold)
   - `[OVERLAP]` tag in TXT output
   - `is_overlap` boolean in JSON output
</action>
  <verify>
**Step 1: Verify imports work**
```bash
python -c "from asr_skill import transcribe, SUPPORTED_VIDEO_FORMATS; print('Imports OK')"
python -c "from asr_skill.preprocessing.video import is_video_file; print(is_video_file('test.mp4'))"
```

**Step 2: Test with a multi-speaker audio file**
```bash
# Use any audio file with multiple speakers
asr-skill path/to/multi_speaker_audio.mp3 -f json -o ./test_output
```

**Step 3: Verify output contains speaker labels**
Check the JSON output for:
- `speaker_id` field in each segment (e.g., "Speaker A", "Speaker B")
- `is_overlap` boolean field
- `speakers` array in response (if using Python API)

**Step 4: Test video file handling (if you have a video file)**
```bash
asr-skill path/to/video.mp4 -f txt -o ./test_output
```

**Expected results:**
- Video file is processed without errors
- Audio is extracted and transcribed
- Speaker labels appear in output
- Temp files are cleaned up (check /tmp for leftover .wav files)
</verify>
  <done>
User confirms:
1. Imports work without errors
2. Speaker labels appear in output (Speaker A, Speaker B, etc.)
3. Overlap detection works (if overlapping speech present)
4. Video files are processed correctly (if tested)
5. Temp files are cleaned up after processing
</done>
  <what-built>
Phase 2 Speaker Diarization implementation with video support, speaker labels, timestamps, and overlap detection.
</what-built>
  <how-to-verify>
See verification steps in task body.
</how-to-verify>
  <resume-signal>Type "approved" if verification passes, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
User confirms:
1. Imports work without errors
2. Speaker labels appear in output (Speaker A, Speaker B, etc.)
3. Overlap detection works (if overlapping speech present)
4. Video files are processed correctly (if tested)
5. Temp files are cleaned up after processing
</verification>

<success_criteria>
- All Phase 2 requirements verified by user:
  - AUDIO-02: Video file input works
  - SPKR-01: Speaker labels appear in output
  - SPKR-02: Speaker timestamps present
  - SPKR-03: Overlap detection works
</success_criteria>

<output>
After completion, create `.planning/phases/02-speaker-diarization/02-04-SUMMARY.md`
</output>
